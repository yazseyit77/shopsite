import React from "react";

const Stock = () => {
  return (
    <div>
      {/* <h2>{props.name}</h2>
      <h4>{props.price}</h4> */}
      <button>Add to cart</button>
      <br />
    </div>
  );
};

export default Stock;
